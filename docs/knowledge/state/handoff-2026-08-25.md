# Session Handoff — 2026-08-25

Read this first when resuming work on a new machine or fresh session.

## Context

A Code Knowledge Graph pipeline was built **outside** this repo at
`~/Developer/ios/codegraph/`:

```
tree-sitter AST extraction  →  JSONL  →  Neo4j (userspace)  →  GraphRAG context packs
        extract.py              graph/*.jsonl   load.py           ask.py
```

Purpose: an AI agent can answer any question about this codebase from the
graph (symbol definitions with kind + file:line, hierarchical summaries,
module context) instead of grepping.

| Phase | Status |
|---|---|
| F0 infra (Neo4j userspace, venv) | ✅ |
| F1 syntactic extraction (validated 6/6 kinds) | ✅ |
| F2 Neo4j load (714 nodes / 799 edges, idempotent) | ✅ |
| F3 curated Browser queries | ✅ |
| F5 hierarchical summaries (138 linked, 0 orphans) | ✅ |
| F6 GraphRAG retrieval (`ask.py`) | ✅ |
| F4 precise references via index-store USRs | 🔲 requires macOS |

## Just landed

`fix: run external processes with timeout to prevent scan hang` —
`FileSystemHelper.runProcess` (concurrent pipe drain + 15s timeout with
terminate→kill escalation). Root cause, tests and expected post-fix behavior:
`tools/codegraph-diagnostics.md`.

## Where we left off

1. Fix committed and verified by regenerating the graph (`ask.py` finds
   `runProcess` at its new location).
2. `codegraph-first` skill committed at `.claude/skills/` so both OpenCode and
   Claude Code load it project-level on any machine.
3. Pending: F4 on macOS (index store USRs → exact CALLS/REFERENCES edges),
   prose enrichment of ~126 file/type summary drafts, CLI parametrization of
   `--repo/--out`.

## First steps on a new machine

1. `git pull` — everything you need travels in the repo.
2. Follow `tools/codegraph-setup.md` to rebuild the workspace at
   `~/Developer/ios/codegraph/` (Neo4j userspace, venv, extract → load →
   summaries).
3. The `codegraph-first` skill loads automatically — prefer the graph over
   grep for symbol lookups.
4. Build the app in Xcode; run a full artifact scan; if the simulator step is
   still slow on the work Mac, follow `tools/codegraph-diagnostics.md`.
